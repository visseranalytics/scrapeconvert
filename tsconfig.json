{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": [".astro/types.d.ts", "**/*"],
  "comment": "Endpoint tests import the workerd-only `cloudflare:test` module; they are typechecked + run by the workers pool (vitest.workers.config.ts) in a workerd env, not by this project-wide tsc gate.",
  "exclude": ["dist", ".wrangler", "src/pages/api/**/*.endpoint.test.ts", "src/server/budgets.test.ts"]
}
